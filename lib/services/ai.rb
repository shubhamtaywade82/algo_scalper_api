# frozen_string_literal: true

module Services
  # AI integration module (Ollama via `ollama-client` gem only)
  # Note: Zeitwerk requires 'Ai' (not 'AI') to match directory name 'ai/'
  module Ai
  end
end
