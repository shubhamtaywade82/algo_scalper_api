# Superpowers Archive

Design specs and plans under `specs/` and `plans/` are **historical**. They often reference
removed **`run_mode`**, **`RUN_MODE`**, and **`config/profiles/*.yml`**.

For current behaviour, use:

- `app/lib/algo_config.rb` — merge order and `LIVE_TRADING` / `SIGNAL_TIER`
- `docs/diagrams/trading-modes.md` — mode diagrams
- `docs/trading/signal_engine.md` — `Signal::Engine` pipeline
